[project]
name = "test-proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.8.0",
]

[tool.uv.extra-build-dependencies]
causal-conv1d = ["torch"]
flash-attn = ["psutil"]